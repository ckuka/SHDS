In this directory you find the necessary configuration for Hadoop.

Start Hadoop distributed filesystem issue the following command (the first command will format the namenode):

/usr/local/bin/hdfs namenode -format
/usr/local/sbin/start-dfs.sh

